name: Premium Interactions

on:
  push:
    branches: [main]
    paths:
      - 'app/premium-liquid-glass.css'
      - 'app/premium-liquid-glass-geometry.css'
      - 'app/premium-interactions.css'
      - 'app/layout.tsx'
      - 'components/PlaceCard.tsx'
      - 'components/PlacePhoto.tsx'
      - 'e2e/premium-interactions.spec.ts'
      - '.github/workflows/premium-interactions.yml'
  workflow_dispatch:

jobs:
  premium-interactions:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_TELEMETRY_DISABLED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Validate place data
        run: npm run validate:data
      - name: Unit and architecture tests
        run: npm test
      - name: Production build
        run: npm run build
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Mobile interaction and zero-Places-call test
        run: npx playwright test e2e/premium-interactions.spec.ts
      - name: Cloudflare dry run
        run: npx wrangler deploy --dry-run
